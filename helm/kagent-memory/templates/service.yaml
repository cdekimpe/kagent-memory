apiVersion: v1
kind: Service
metadata:
  name: {{ include "kagent-memory.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kagent-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kagent-memory.selectorLabels" . | nindent 4 }}
